# =============================================================================
# XPNC Environment Variables
# Copy this file to .env and fill in your values.
# Vite only exposes variables prefixed with VITE_ to the client.
# =============================================================================

# Supabase — Database and Auth
# Get these from: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Privy — Web3/Email Authentication
# Get your App ID from: https://dashboard.privy.io
VITE_PRIVY_APP_ID=your_privy_app_id

# xAI — AI Impact Scoring (optional but recommended)
# Get your API key from: https://console.x.ai
# Without this, submissions get a fallback score instead of AI analysis
VITE_XAI_API_KEY=your_xai_api_key

# xAI Model (optional, default: grok-4-fast-reasoning)
# VITE_XAI_MODEL=grok-4-fast-reasoning

# Admin — Secret key to access /admin panel
# No link in the UI; go directly to /admin and enter this key
VITE_ADMIN_SECRET=your_admin_password
